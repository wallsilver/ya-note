[pytest]
DJANGO_SETTINGS_MODULE = yanote.settings
testpaths = notes/pytest_tests
addopts = -rE -vv --show-capture=no --disable-warnings -p no:cacheprovider
# Отключения вывода предупреждений о устаревшей версии
filterwarnings = ignore::DeprecationWarning:_pytest.assertion.rewrite